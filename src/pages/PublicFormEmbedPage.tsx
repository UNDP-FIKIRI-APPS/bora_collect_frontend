import PublicFormPage from './PublicFormPage';

/** Version embarquable (iframe) — chrome minimal, style Tally embed */
export default function PublicFormEmbedPage() {
  return <PublicFormPage embedMode />;
}
