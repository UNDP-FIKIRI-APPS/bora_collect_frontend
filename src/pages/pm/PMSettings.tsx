import React from 'react';
import ProfileSettingsCore from '../../components/settings/ProfileSettingsCore';

const PMSettings: React.FC = () => (
  <ProfileSettingsCore showPMFields pageTitle="Paramètres Chef de projet" />
);

export default PMSettings;
